[project]
name = "thermal-presence-lab-challenge"
version = "0.1.0"
description = "TinyML Thermal Presence Detection — Lab Challenge"
requires-python = ">=3.13"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "tensorflow",
]
